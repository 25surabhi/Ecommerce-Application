extends layout

block content
	h1= title
	p Welcome to #{title}
	br
	each product, i in car
		<p>#{product.Description}</p>
		</br>
		<img src =#{product.Image} height=200 width=200></img>
		
		<p>#{product.Model}</p>
		<p>#{product.Year}</p>
		<p>#{product.Price}</p>
		

